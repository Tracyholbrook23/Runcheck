# Session Handoff — 2026-03-19 (Evening)

---

## What Changed This Session

### Modified Files

| File | What changed |
|------|-------------|
| `App.js` | Swapped `createBottomTabNavigator` → `createMaterialTopTabNavigator`. Added `tabBarPosition="bottom"`, `swipeEnabled: true`, `tabBarIndicatorStyle: { height: 0 }`. Swipe left/right between tabs now works. `registerPushToken()` wired in `MainTabs` useEffect. |
| `app.json` | Added `expo-notifications` plugin with `{ "color": "#E94E1B" }`. Added `NSUserNotificationUsageDescription` to iOS infoPlist. |
| `utils/notifications.js` | Added `projectId` to `getExpoPushTokenAsync()` call — required for Expo SDK 50+. Without this, token registration silently fails. |
| `screens/ProfileScreen.js` | Gradient avatar ring (LinearGradient: `#FF4500 → #CC1100 → #1A0000 → #FF6B00`). Unified rank card: rank icon + name + skill badge + points + progress bar + "X pts to next rank" + leaderboard button — all in one bordered card. |
| `screens/UserProfileScreen.js` | Applied same gradient avatar ring to other users' profiles. Added `LinearGradient` import, wrapped avatar in ring, added `avatarRing` / `avatarInner` / `avatarImage` styles. |
| `screens/PremiumScreen.js` | Monthly/Annual pricing cards are now `TouchableOpacity`. Added `selectedPlan` state (default: `'annual'`). Selected card gets orange border highlight. CTA button updates to show selected plan price. |
| `PARKING_LOT.md` | Added **Run Chat** spec — per-run group chat for RSVP'd players, Firestore `messages` subcollection, push notification fan-out via Cloud Function. |

---

## What Is Now Working

- **Swipe tabs** — Swipe left/right between Home, Runs, Check In, Plan, Profile on physical device (requires new native build — shipped in today's EAS build)
- **Push notifications wired** — Permission prompt fires on first launch, token saved to `users/{uid}.pushToken` in Firestore
- **Gradient avatar ring** — Orange/red/dark gradient ring on own profile (ProfileScreen) and other users' profiles (UserProfileScreen)
- **Rank card redesign** — Unified rank card on ProfileScreen: rank + points + progress bar + skill badge all in one cohesive block
- **Premium plan toggle** — Monthly/Annual cards are tappable and selection is visually tracked

---

## What Is Still In Progress / Pending Verification

- **Push notification on-device test** — Build is on TestFlight. Need to:
  1. Install from TestFlight
  2. Confirm permission prompt fires on launch
  3. Confirm `pushToken` field appears in Firestore under `users/{uid}`
- **APNs key** — Uploaded to Firebase Cloud Messaging for iOS app ✅ (confirmed by user)
- **Swipe tabs on physical device** — Works in simulator, needs verification on TestFlight build
- **App Store screenshots** — Still not captured (pending)
- **QA proximity check-in on device** — Still pending

---

## EAS Build Info

- Build type: iOS internal distribution
- Commit: includes swipe tabs + push notifications + projectId fix
- Status: Completed and available on TestFlight as of 2026-03-19 ~2:46 PM

---

## Next Steps

1. **Install TestFlight build → test push notification prompt + token write to Firestore**
2. **Test swipe tabs on physical device**
3. **QA proximity check-in on device**
4. **App Store screenshots** — last remaining visual launch task
5. **Run Chat** — parked, build when push notifications are verified working (needs notification fan-out Cloud Function)
6. **Terms of Service screen** — button exists in SettingsScreen, needs content wired up before App Store submission
