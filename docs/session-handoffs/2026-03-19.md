# Session Handoff — 2026-03-19

_Generated by nightly-memory scheduled task_

---

## What Changed

### New Files Added

| File | What it is |
|------|------------|
| `screens/CreatePrivateRunScreen.js` | UI-only Premium teaser for Private Run and Paid Run features. Full interactive form: gym, date, max players, skill level, entry fee with live payout calculator (entryFee × maxPlayers × 0.9). CTA triggers "Coming Soon / Premium Only" modal → routes to PremiumScreen. No Firestore writes. |
| `screens/AdminAllClipsScreen.js` | Admin clip browser. Filter modes: All (newest 25), By Gym, Hidden, Featured. Real-time `onSnapshot` listener. Cards show thumbnail, uploader, gym, status badges. Feature/Unfeature/Hide/Unhide via `callFunction`. Gated by `useIsAdmin`. |
| `hooks/useProximityCheckIn.js` | Smart proximity check-in hook. Polls GPS every 30s while foregrounded, re-checks on foreground event. Surfaces `nearbyGym` when user is inside a gym's `checkInRadiusMeters`. 30-min per-gym dismiss cooldown. Accuracy gate: ignores GPS fixes > 100m. Dev bypass respected. |
| `utils/locationUtils.js` | GPS utility module: `isLocationGranted`, `requestLocationPermission`, `getCurrentLocation`, `calculateDistanceMeters`. Uses geofire-common. Dev mode returns Cowboys Fit coordinates. |
| `utils/haptics.js` | Haptic feedback helpers with no-op fallbacks: `hapticSuccess`, `hapticLight`, `hapticMedium`, `hapticHeavy`. |

### Modified Files (Feature Drop — commit 458174f, 04:33 AM)

| File | What changed |
|------|-------------|
| `screens/HomeScreen.js` | Added prominent "Start a Group Run" card with frosted-glass style and orange accent. "Check Into a Run" card is now contextual: shows today's scheduled gym name + time, taps navigate to RunDetails. Subtitle changed from "Find courts near you" to accurate GPS copy. |
| `screens/RunDetailsScreen.js` | Collapsible gym hours block (today's hours + full week expand). "Visit Gym Website" button gated on `gym.websiteUrl`. "Start a Run" now opens run type picker sheet (Open / Private / Paid). Verbose [RunDetails] debug log block removed. Also integrated `useProximityCheckIn` + haptic feedback (earlier commit). |
| `screens/ViewRunsScreen.js` | Replaced pill filter strip with "Filter" button + bottom sheet. Sheet filters: Court Type, Access (All/Free/Membership), Sort by Nearest. Active filters shown as dismissible chips below button. |
| `screens/PremiumScreen.js` | Free/Premium tab toggle. Free tab: feature list with checkmarks + limit chips. Premium tab: side-by-side comparison table + pricing + feature cards. |
| `App.js` | `CreatePrivateRun` added to RunsStack. `Premium` added to RunsStack. `AdminAllClips` added to ProfileStack. |
| `screens/CheckInScreen.js` | Integrated `useProximityCheckIn` + haptic feedback. |
| `screens/RecordClipScreen.js` | Added `hapticMedium` on record start. |
| `screens/TrimClipScreen.js` | Added `hapticSuccess` on successful clip post. |
| `hooks/index.js` | Added `useProximityCheckIn` to barrel export. |
| `hooks/useProximityCheckIn.js` | Removed verbose [PROXIMITY] debug logs. Behavior unchanged. |
| `utils/locationUtils.js` | Removed [DISTANCE] debug log. Behavior unchanged. |

---

## What Is Now Working

- **Proximity check-in** — Hook built, exported, integrated in CheckInScreen and RunDetailsScreen. Users within a gym's check-in radius see a one-tap prompt.
- **AdminAllClipsScreen** — Fully functional admin tool for browsing, filtering, and managing all clips. Registered in nav.
- **HomeScreen "Start a Group Run" card** — Prominent CTA on dashboard opens run type picker.
- **HomeScreen contextual check-in card** — When user has a scheduled visit today, card shows gym name + time and navigates directly to RunDetails.
- **RunDetailsScreen gym hours + website** — Collapsible hours and optional website button are live.
- **ViewRunsScreen filter sheet** — Bottom-sheet filter with Court Type, Access, and Sort replaces old pill strip.
- **PremiumScreen redesign** — Free/Premium comparison UI is live.
- **CreatePrivateRunScreen** — Premium teaser with payout math is live and reachable from RunsStack.
- **Haptics** — Check-in, record, and clip-post interactions have tactile feedback.

---

## What Is Still In Progress

- **Proximity check-in UI** — Hook is integrated but visual prompt in CheckInScreen and RunDetailsScreen has not been verified on-device.
- **Backend Cloud Functions (not yet deployed)** — The following are code-complete but need a deploy from `~/Desktop/runcheck-backend`:
  - `detectNoShows` (new scheduled function, every 15 min)
  - `onScheduleWrite` (cancel threshold aligned to 60 min)
  - `checkIn` and `createRun` (suspension guards — code written 2026-03-17, deployed 2026-03-18 per checklist)
- **Push notifications** — Not yet implemented. Blocking the TestFlight build (being batched).
- **TestFlight build** — Not triggered. Batching with push notifications to avoid two native rebuilds.
- **App Store screenshots** — Not yet captured.

---

## Bugs / Risks

- **`CreatePrivateRunScreen` is technically out-of-scope** — Added outside the launch checklist. UI-only (no Firestore writes), so no backend risk, but adds surface area. Confirmed intentional by session activity.
- **`useProximityCheckIn` not device-verified** — Dismiss cooldown resets on app kill (acceptable for MVP). The prompt card + CTA flow haven't been independently QA'd on device.
- **No tests for new files** — `useProximityCheckIn`, `locationUtils`, `haptics`, `AdminAllClipsScreen`, `CreatePrivateRunScreen` have no tests in `__tests__/`. Not a launch blocker.
- **`gym.websiteUrl` and `gym.hours` fields** — RunDetailsScreen now references these fields, but they are not in the BACKEND_MEMORY.md schema for `gyms/{gymId}`. Confirm these fields are being seeded and query gracefully when absent.
- **`useProximityCheckIn` dismiss state resets on app reload** — In-memory `useRef`. Re-prompts after kill within 30-min window. Acceptable for MVP.

---

## Next Steps

1. **Add `websiteUrl` and `hours` to BACKEND_MEMORY gym schema** — RunDetailsScreen references these fields; document them to prevent future schema drift.
2. **QA proximity check-in on device** — Verify the gym prompt appears and the "Check In" CTA completes from both CheckInScreen and RunDetailsScreen.
3. **Deploy backend Cloud Functions** — `detectNoShows`, `onScheduleWrite` (cancel threshold). Suspension guards for `checkIn` and `createRun` were deployed 2026-03-18.
4. **Push notifications + TestFlight** — Bundle `expo-notifications` with the native rebuild; trigger EAS build when ready.
5. **App Store screenshots** — Last remaining visual launch task.
6. **Verify AdminAllClipsScreen in production** — Confirm `callFunction('featureClip' / 'hideClip')` resolves correctly.
