# Session Handoff — 2026-03-24

_Generated by nightly-memory task._

---

## What Changed

A large session covering three areas: (1) Run Level UI polish (Phases 2/3/4 + competitive meter skill snapshot fix), (2) DM system hardening (blocking, muting, message-level reporting, admin enforcement), and (3) reliability threshold + moderation safety fixes.

---

### Run Level Phase 2 / 3 / 4 + Competitive Meter Skill Snapshot

**ViewRunsScreen.js** — Gym cards now show a run level badge (Competitive / Balanced / Casual) when at least one upcoming run exists at that gym. Dominant level computed from `allUpcomingRuns` (already subscribed). `runLevelInfoVisible` state + a second bottom-sheet modal with "Got it" button.

**RunDetailsScreen.js** — (Phase 3) "Mixed" renamed to "Balanced" everywhere in the UI; `runLevel` value in Firestore stays `'mixed'`. Competitive Meter added: 5 compact bars (12×5px each), formula `Competitive→5 / Either→3 / Casual→1`, average rounded, color-coded red/slate/green. (Phase 4) Run style badges and meter bars are tappable — shared `infoSheetType` state drives one Modal bottom sheet with contextual explanations.

**services/runService.js** — `fetchUserDisplayInfo` now reads `skillLevel` from `users/{uid}` and includes it in the returned object. `joinRun` (internal) writes `skillLevel: userInfo.skillLevel || null` onto the `runParticipants` doc at join time.

**RunDetailsScreen.js (skill snapshot fix)** — `getCompetitiveBars()` now prefers `participant.skillLevel` (V2 snapshot path) before falling back to presence cross-reference (V1 for older docs). Fixes meter showing "Balanced" for all upcoming runs with no check-ins.

---

### DM User Blocking

**services/dmService.js** — Added `blockUser(currentUid, targetUid)` and `unblockUser`. Block guard added to `sendDMMessage` (reads recipient's `blockedUsers`). Bidirectional block guard in `openOrCreateConversation` slow path (reads both user docs in parallel; throws if either party blocked the other).

**screens/UserProfileScreen.js** — Block/Unblock button added below Message button. `isBlocked` state read from `currentUserSnap.blockedUsers`. Friend and Message buttons hidden when blocked.

**screens/DMConversationScreen.js** — `handleSend` now passes `recipientId: otherUserId` to `sendDMMessage`.

**Schema addition** — `users/{uid}.blockedUsers: string[]`

---

### DM Conversation Mute

**services/dmService.js** — Three new functions: `muteConversation`, `unmuteConversation`, `getConversationMuteState`. Dot-notation writes to `conversations/{id}.mutedBy.{uid}`.

**screens/DMConversationScreen.js** — Mute bell icon added to header alongside flag button. `isMuted` state initialized on mount via `getConversationMuteState`. Optimistic toggle with revert on error.

**runcheck-backend/functions/src/onDmMessageCreated.ts** — Mute guard: reads `conversationData.mutedBy?.[recipientUid]` (conversation doc already loaded — zero extra reads). Returns early (skips push, skips cooldown) if muted.

**screens/MessagesScreen.js** — Mute indicator (bell-off icon) shown next to timestamp on muted DM conversations.

**Schema addition** — `conversations/{id}.mutedBy: { [uid]: true } | undefined`

---

### Message-Level DM Reporting

**runcheck-backend/functions/src/submitReport.ts** — Added `'message'` to `VALID_TYPES`. `MessageContext` interface validated and stored on report doc. `targetOwnerId` set to `messageContext.senderId` for message reports.

**components/ReportModal.js** — Accepts optional `messageContext` and `blockSenderId` props. Shows italicized message preview for `type='message'`. "Also block this user" toggle (shown for message reports with `blockSenderId`). After successful report, calls `blockUser` if toggle is checked.

**screens/DMConversationScreen.js** — Long-press on non-own messages opens `ReportModal` with `messageContext`. `reportTarget` state. Second `<ReportModal>` for message-level reports (separate from header-level player report).

**screens/AdminReportsScreen.js** — Cyan "Message" type badge + quoted excerpt block for message reports. Sender/Received by/Sent meta rows (zero extra reads — all data already on report doc).

---

### Admin DM Message Enforcement

**runcheck-backend/functions/src/moderationHelpers.ts** — Added `enforceRemoveDmMessage(db, conversationId, messageId, actor, reason)`. Soft-deletes message (`isRemoved: true` + `removedBy`, `removedAt`, `removedReason`). Idempotent.

**runcheck-backend/functions/src/removeDmMessage.ts** — NEW admin-only `onCall` CF. Accepts `{ conversationId, messageId, reason?, reportId? }`. Calls `enforceRemoveDmMessage` → optionally `resolveRelatedReport`. Writes to `adminActions/{autoId}`.

**runcheck-backend/functions/src/index.ts** — Added `removeDmMessage` export.

**screens/DMConversationScreen.js** — `MessageBubble` early-return for `message.isRemoved === true` — renders pill-style placeholder. No long-press on removed messages.

**screens/AdminReportsScreen.js** — "Remove Message" action button for `type='message'` reports.

**Schema addition** — `conversations/{id}/messages/{autoId}`: `isRemoved`, `removedBy`, `removedAt`, `removedReason` fields.

**New collection** — `adminActions/{autoId}` (audit log — Cloud Function writes only, no client access).

---

### Repeat-Offender Badge in AdminReportsScreen

**screens/AdminReportsScreen.js** — `suspensionLevels` state map populated from user docs already loaded by `resolveLabels` (zero extra reads). Report cards show "Currently Suspended · Lvl N" (red) or "Repeat Offender · Lvl N" (amber) badge for subjects with suspension history.

---

### Messaging & Moderation Hardening Pass

**runcheck-backend/firestore.rules** — `isNotSuspended()` helper function reads `users/{uid}.isSuspended` + `suspensionEndsAt` vs `request.time`. Applied to `allow create` on DM messages and run chat messages. Server-enforced: suspended users cannot write messages even if they bypass the client.

**services/dmService.js** — `openOrCreateConversation` slow path now reads both user docs in parallel. Bidirectional block guard.

**runcheck-backend/functions/src/removeDmMessage.ts** — Fire-and-forget write to `adminActions/{autoId}` after successful message removal.

**runcheck-backend/functions/src/suspendUser.ts** — Fire-and-forget write to `adminActions/{autoId}` after successful suspension.

---

### Reliability Threshold + useReliability Hook Rebuild

**hooks/useReliability.js** — Rebuilt with `displayScore` (pinned to 100 until `RELIABILITY_THRESHOLD = 3` attended runs, matching backend computeScore lock), `displayTier`, `meetsThreshold`. Uses `onSnapshot` on `users/{uid}` for real-time updates. Exposes `score` (real backend value) separately from `displayScore` (UI value).

**services/reliabilityService.js** — Updated `NO_SHOW_PENALTY` (10 → 20), added `LATE_CANCEL_PENALTY: 8` to match backend formula. `calculateReliabilityScore` now uses `totalLateCancelled` in formula.

**screens/ProfileScreen.js** — Uses `useReliability()` hook exclusively. `displayScore`/`displayTier` from hook. RC-004 fixed: stats no longer flash zeroes because reliability data comes from the same hook subscription. Reliability info modal ("How Reliability Works") added.

**screens/UserProfileScreen.js** — Reliability display pinned to 100 until `totalScheduled >= 3` (same threshold). Reliability info modal added. Block/Unblock button added.

---

### PlanVisitScreen Start-a-Run Prompt

**screens/PlanVisitScreen.js** — Step 4 (confirmation screen) now shows "Want others to join you?" prompt with a "Start a Run" button. Calls `startOrJoinRun` (same merge rule as RunDetailsScreen), then navigates to RunDetailsScreen. Wizard state reset after navigation.

---

### Phase 1 Moderation Safety Fixes

**screens/DMConversationScreen.js** — Flag button added to header (opens `ReportModal` with `type='player'`, `targetId=otherUserId`).

**services/dmService.js** — `sendDMMessage` reads sender's user doc and throws if `isSuspended === true` and suspension hasn't expired.

**runcheck-backend/functions/src/submitReport.ts** — Auto-mod threshold query now adds `.where('status', '==', 'pending')` — only pending reports count toward threshold. Resolved/reviewed reports no longer re-trigger enforcement.

---

## What Is Now Working

- Run level badges on ViewRunsScreen gym cards (dominant level across upcoming runs)
- "Balanced" label replacing "Mixed" everywhere in UI (Firestore value unchanged)
- Competitive Meter on run cards with tappable info sheet
- Competitive Meter using skill snapshots from `runParticipants` (no longer showing Balanced for all upcoming runs)
- DM user blocking: block/unblock from UserProfileScreen; send blocked by service guard
- DM conversation mute: bell toggle in chat header; push skipped when muted
- Message-level reporting: long-press message → report with context + optional block
- Admin: Remove Message action on message reports; isRemoved placeholder in chat
- `adminActions` audit log for message removal and user suspension
- Reliability score pinned to 100 until 3 processed events (matches backend)
- ProfileScreen RC-004 stats flash fixed (unified hook)
- PlanVisitScreen confirmation step shows "Start a Run" CTA
- Server-enforced suspension check on DM and run chat message creates (Firestore rules)
- Auto-moderation now only counts pending reports toward thresholds (not reviewed/resolved)

---

## What Is Still In Progress / Unfinished

1. **Firestore rules for `conversations` collection not written** — Security gap: any authenticated user can read/write any DM conversation. `isNotSuspended()` create rule was added but broader read/write collection rules are still missing. **Do not invite external testers to use DMs.**

2. **Firestore rules for `usernames` collection not written** — Any authenticated user can overwrite another user's username reservation. Low immediate risk, must be closed before launch.

3. **Run Chat Firestore rules written but not yet deployed** — Device QA of Run Chat is fully blocked (`permission-denied`). Deploy: `cd ~/Desktop/runcheck-backend && firebase deploy --only firestore:rules`

4. **Username not displayed on any screen** — `username` is stored and shown read-only in EditProfileScreen; not visible on ProfileScreen, UserProfileScreen, PresenceList, or run cards.

5. **OTA channel fix** — Current TestFlight binary missing OTA channel header. Fresh build needed when EAS quota resets (~April 1).

6. **DMs + Messages inbox** — Simulator-tested only. Not verified on physical device.

7. **OnboardingRegionScreen** — Not tested in full device flow.

8. **App Store screenshots** — Not prepared.

9. **`onDmMessageCreated` CF** — Mute guard was added (function exists), but full DM push notification pipeline readiness (correct payload, tap-to-navigate from notification) needs device testing.

---

## Bugs / Risks

- **DM security gap** — Do not open DMs to external testers until `conversations` collection rules are deployed.
- **Run Chat permission-denied on device** — Rules written, not deployed. Completely broken on physical device.
- **V1 blocking limitation** — Block guard in `openOrCreateConversation` is client-side only. Firestore rules do not enforce block state on conversation creation (would require reading both user docs in rules — expensive).
- **Skill snapshot backward compat** — Participant docs written before 2026-03-24 have no `skillLevel` field. Those runs continue to use the presence/runLevel fallback for the meter until participants leave and rejoin.
- **Mute V1 limitations** — No mute expiry (indefinite). No "you've muted this" banner in the chat view. Mute indicator appears in MessagesScreen inbox but not on the conversation header itself.
- **reliabilityService.calculateReliabilityScore** uses `totalLateCancelled` field but `users/{uid}.reliability` currently only has `totalScheduled`, `totalAttended`, `totalNoShow`, `totalCancelled`. Verify backend Cloud Functions write `totalLateCancelled` to the user doc — if not, the display fallback may produce incorrect results.

---

## Next Steps (Priority Order)

1. **Deploy Run Chat Firestore rules** — one-command fix, unblocks device QA: `cd ~/Desktop/runcheck-backend && firebase deploy --only firestore:rules`
2. **Write `conversations` Firestore rules** — security blocker for DMs (read/write gates on participant membership).
3. **Write `usernames` Firestore rules** — write-only for owner uid.
4. **Test DMs + Messages inbox on device** after rules are deployed.
5. **Display `username` on ProfileScreen / UserProfileScreen** — makes the username visible after it's claimed.
6. **Verify `totalLateCancelled` in backend** — confirm Cloud Functions write this field to `users/{uid}.reliability`; if not, fix `calculateReliabilityScore` display fallback.
7. **Prepare App Store screenshots** on simulator (iPhone 15 Pro or 16 Max).
8. **Fresh iOS build when EAS quota resets (~April 1)** — picks up all changes since 2026-03-19 build.
