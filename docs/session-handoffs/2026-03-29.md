# Session Handoff — 2026-03-29

---

## What Changed

### Gym Batch 2 — 10 New Gyms Added and Seeded

**`Runcheck/seedProductionGyms.js`** — 10 new gym entries appended. Total is now **19 gyms**. All validated and seeded to Firestore with `node seedProductionGyms.js`. Confirmed live in the app (visible on Find a Run screen).

New gyms added:
- `ymca-northwest-austin` — YMCA of Austin - Northwest Branch (paid, indoor, Austin)
- `ymca-downtown-austin` — YMCA of Austin - Downtown (paid, indoor, Austin)
- `ymca-round-rock` — YMCA of Austin - Round Rock (paid, indoor, Round Rock)
- `cedar-park-recreation-center` — Cedar Park Recreation Center (paid, indoor, Cedar Park)
- `la-fitness-cedar-park` — LA Fitness - Cedar Park (paid, indoor, Cedar Park)
- `dittmar-recreation-center` — Dittmar Recreation Center (free, indoor, Austin PARD)
- `metz-recreation-center` — Metz Recreation Center (free, indoor, Austin PARD)
- `northwest-recreation-center-austin` — Northwest Recreation Center (free, indoor, Austin PARD)
- `old-settlers-park-round-rock` — Old Settlers Park (free, outdoor, Round Rock)
- `la-fitness-south-austin` — LA Fitness - South Austin (paid, indoor, Austin)

Batch 2 gyms do **not** yet have `placeId`, `website`, `imageUrl`, or `photoGallery` — the app shows a placeholder image for all 10. Enrichment pipeline must be run separately (see below).

---

### Enrichment Pipeline Scripts Updated

**`runcheck-backend/scripts/enrichGymsWithPlaces.js`** — Three fixes:
1. `SOURCE_GYMS` array updated to include all 10 Batch 2 gyms (with `id`, `name`, `address`, `city`, `state`, `zip`, `location`).
2. dotenv path corrected: now loads from `scripts/.env` (not `../`.env — no root `.env` exists).
3. `const API_KEY` updated to `process.env.GOOGLE_PLACES_API_KEY || process.env.GOOGLE_MAPS_API_KEY` — the env file uses `GOOGLE_MAPS_API_KEY`, not `GOOGLE_PLACES_API_KEY`.

**`runcheck-backend/scripts/downloadGymPhotos.js`** — No changes needed; dotenv was already correct.

**`runcheck-backend/scripts/seedGyms.js`** — No changes needed; dotenv was already correct.

---

## What Is Now Working

- 19 gyms live in Firestore and visible in the Find a Run screen
- Seed script validated clean (`--validate` passes with warnings only — warnings are false-positives about Firebase Storage URLs being "external")
- Enrichment pipeline scripts ready to run on Batch 2 gyms

---

## What Is Still In Progress / Unfinished

1. **Batch 2 gym photos** — Run enrichment pipeline:
   ```bash
   cd ~/Desktop/runcheck-backend
   node scripts/enrichGymsWithPlaces.js
   node scripts/downloadGymPhotos.js
   node scripts/seedGyms.js
   ```
2. **Batch 2 coordinate verification** — All 10 Batch 2 gyms have best-effort coordinates from training data. Verify each by right-clicking the building in Google Maps → "What's here?" and updating `seedProductionGyms.js` if needed.
3. **Fresh iOS build** — Pending EAS quota reset (~April 1). Current TestFlight binary missing OTA channel header.
4. **Full real-device QA pass** — After fresh build: onboarding flow, AdminAllClipsScreen Hidden/Deleted tabs, ClipPlayerScreen buffering states.

---

## Carry-forward from 2026-03-28

- Firestore indexes deployed (isHidden+hiddenAt, isDeletedByUser+deletedAt) ✅
- expireClips raw-file guard deployed ✅
- Broken clip `presence_cowboys-fit-pflugerville_SMQUyWWMUOZpBHYN7pWlt15b6CB3` manually hidden ✅

---

## Next Steps (Priority Order)

1. Run enrichment pipeline on Batch 2 gyms (see above)
2. Wait for EAS quota reset (~April 1), trigger fresh iOS build
3. Full real-device QA pass after build
4. Verify Batch 2 coordinates via Google Maps pin-drop
