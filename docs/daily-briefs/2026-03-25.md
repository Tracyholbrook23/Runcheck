# RunCheck Daily Brief — 2026-03-25

_Auto-generated. Source: PROJECT_MEMORY.md, BACKEND_MEMORY.md, session-handoffs/2026-03-23.md_

---

## Current Phase

**Pre-Launch Hardening.** Core features are complete. The blocker is Firestore security rules — three gaps must be closed before any real users access DMs or Run Chat on device. After rules are deployed, the focus shifts to device QA and a clean TestFlight build.

---

## What Is Working

- Full auth flow: signup (with username) → email verify → onboarding → Main
- Username system: new users + `ClaimUsernameScreen` migration for existing accounts
- Run lifecycle: start/join/leave, participant counts, run levels (all 4 phases complete)
- Check-in / check-out flow (GPS distance check is disabled for testing)
- Activity feed, Home screen live runs section
- DM system: conversations, messaging, mute, block, report+block combo
- Run Chat: participants-only, 4hr expiry, unread detection
- Push notifications Phase 1: deployed (run reminders, join alerts, milestones)
- Moderation pipeline: auto-mod thresholds, admin dashboards, suspension escalation
- Admin enforcement: hide clip, remove run, remove DM message, suspend/unsuspend user
- Clip posting, tagging, approval; weekly free-tier cap
- Weekly winners, leaderboard, player reviews
- OTA channel fix applied to `eas.json` (takes effect on next fresh build)

---

## What Is In Progress / Incomplete

| Item | Status |
|------|--------|
| Firestore rules: `usernames` collection | **Not written** — security gap |
| Firestore rules: `conversations` + messages subcollection | **Not written** — security gap |
| Run Chat Firestore rules | **Written, NOT deployed** — permission-denied on device |
| `onDmMessageCreated` Cloud Function | **Not built** — DM push notifications won't fire |
| Username displayed in UI | Not shown anywhere except EditProfileScreen (read-only) |
| DMs + Messages inbox | Simulator-verified only — not tested on device |
| OnboardingRegionScreen | Not tested in full device flow |
| App Store screenshots | Not prepared |
| Fresh iOS build | Pending EAS quota reset ~April 1 |

---

## Issues / Risks

**SECURITY (act now):**
- Any authenticated user can overwrite another user's username reservation (`usernames` rules missing)
- Any authenticated user can read/write any DM conversation (`conversations` rules missing)
- Do NOT share the app with external testers until both rule sets are deployed

**DEVICE QA BLOCKER:**
- Run Chat throws `permission-denied` on device — rules are written but not deployed

**BUILD:**
- OTA updates do not reach current TestFlight users — frontend bug fixes cannot ship until a fresh build (~April 1)
- Use simulator for frontend validation until then

**MINOR:**
- `SignupScreen.js` JSDoc comment is stale (describes old Firestore-write-in-signup flow)
- `notifCooldowns` map will grow unboundedly after ~500 active users (track before marketing push)

---

## TOP 3 Priorities Today

### 1. Deploy Run Chat Firestore Rules
The rules already exist in `runcheck-backend/firestore.rules`. Just deploy.

```bash
cd ~/Desktop/runcheck-backend
firebase deploy --only firestore:rules
```

Verify: open Run Chat on a physical device as a joined participant — should load without `permission-denied`.

**Files:** `runcheck-backend/firestore.rules`

---

### 2. Write + Deploy Firestore Rules for `conversations` Collection

Add rules to `runcheck-backend/firestore.rules`. Pattern:
- `conversations/{conversationId}`: read/write only if `request.auth.uid in resource.data.participantIds`
- `conversations/{conversationId}/messages/{messageId}`: create if `request.auth.uid in get(/conversations/{conversationId}).data.participantIds`; update/delete always denied
- Also apply the `isNotSuspended()` helper already defined in the rules for message creates

Deploy immediately after writing.

**Files:** `runcheck-backend/firestore.rules`

---

### 3. Write + Deploy Firestore Rules for `usernames` Collection

Add rules to `runcheck-backend/firestore.rules`. Pattern:
- Only the uid in the doc can write their own reservation: `request.auth.uid == request.resource.data.uid`
- No reads needed by other clients (reservation checks are done via transaction writes that either succeed or fail)

Deploy with the same `firebase deploy --only firestore:rules` command after writing.

**Files:** `runcheck-backend/firestore.rules`

---

## After the Top 3 Are Done

Once all three rule sets are deployed and Run Chat works on device:

4. **Test DMs + Messages inbox on device** — first real device QA of the DM system
5. **Display username on ProfileScreen / UserProfileScreen** — make the username visible to users after they claim it
6. **Build `onDmMessageCreated` Cloud Function** — completes the DM push notification pipeline

---

## What to AVOID Today

- **New features** — no scope creep. Run Level is done (4 phases). Verified Run History is deferred post-launch.
- **App Store screenshots** — do not start this until device QA is clean
- **Clip feature expansion** — explicitly out of scope for current milestone
- **Private run / monetization work** — out of scope
- **Fresh build** — don't burn EAS build quota; wait for ~April 1 quota reset
- **Reliability service refactor** — Cloud Functions own reliability writes; leave it alone
- **Admin UI additions** — admin tools are sufficient for launch; resist the urge to expand them

---

## Exact Files to Start With

```
~/Desktop/runcheck-backend/firestore.rules   ← Add rules for conversations + usernames; then deploy
```

After deploy:
```
screens/ProfileScreen.js       ← Add username display (below display name)
screens/UserProfileScreen.js   ← Same
```

---

_Next milestone gate: all three Firestore rule gaps closed → device QA of DMs + Run Chat → fresh build when EAS quota resets (~April 1)._
