# Daily Brief — 2026-03-21

_Generated by morning-brief-700-am scheduled task._

---

## Current Phase

**Pre-launch hardening.** Core features are built. The goal is to close security gaps, deploy pending backend rules, and get the next iOS build ready for TestFlight submission when the EAS quota resets (~April 1).

---

## What Is Working

- Auth flow end-to-end: signup → email verification → username claim → onboarding (5-step, now includes Region Notice) → Main
- Check-in / check-out with GPS enforcement, presence writes, points, activity feed
- Gym discovery, home court selection, live player counts (real-time)
- Run creation/join/leave with participant sync and merge rule
- Run Chat MVP — code complete, wired, tested in simulator
- DM System — code complete: conversations, inbox, 1:1 chat, unread badges, notification tap handler
- Push notifications Phase 1 — permission prompt, token save, 3 Cloud Functions deployed (`notifyRunStartingSoon`, `onRunParticipantJoined`, `onParticipantCountMilestone`)
- Clips: record → trim → upload → playback (on-device trimming, weekly cap, soft-delete)
- Moderation & admin: report pipeline, auto-moderation, suspension escalation, admin screens
- Weekly leaderboard + winners (automated Cloud Function + manual script)
- Premium UI teaser (no billing)
- TestFlight build live (2026-03-19)

---

## What Is In Progress / Not Yet Done

1. **Firestore rules for `conversations` collection** — not written. DMs are currently open to any authenticated user. Security gap. Must be done before any real users access the feature.
2. **Run Chat Firestore rules** — written in the backend repo but not deployed. Any user who opens Run Chat on device gets `permission-denied`. Carried over from 2026-03-20.
3. **`onDmMessageCreated` Cloud Function** — not built. DM push notifications won't fire. App.js tap handler is wired and waiting.
4. **Fresh iOS build** — OTA updates do not apply to the current TestFlight binary (missing channel header). Fix is in `eas.json`. Next build will resolve it. EAS quota resets ~April 1.
5. **App Store screenshots** — not prepared. Last unchecked item on the launch checklist.
6. **DMs + Messages inbox on-device testing** — only simulator-verified so far. Needs to run with rules deployed.
7. **OnboardingRegionScreen** — not yet tested in the full auth flow on device.

---

## Issues / Risks

- **DM security gap is the most urgent risk.** Do not publicize or invite users to test DMs until `conversations` Firestore rules are deployed.
- **Run Chat permission-denied on device** — blocks QA of the Run Chat MVP on real hardware.
- **OTA updates broken on current binary** — any frontend-only bug fixes can't ship until the next build. Use simulator for frontend validation.
- **`notifCooldowns` growing unboundedly** — migrate to subcollection before ~500 active users (logged in BACKEND_MEMORY Known Issues).
- **`sendDMMessage` is not transactional** — `lastMessage` can lag under race conditions. Acceptable at MVP scale.

---

## TOP 3 Priorities for Today

### 1. Deploy Firestore rules for Run Chat + write rules for Conversations
**Files:** `~/Desktop/runcheck-backend/firestore.rules`
- Run Chat rules are written — just run `firebase deploy --only firestore:rules` from the backend repo.
- Write `conversations` rules: read + create require `request.auth.uid in resource.data.participantIds`. Messages subcollection: same check against parent conversation doc.
- Deploy both together in one pass.
- **Why:** Unblocks on-device QA of Run Chat. Closes the DM security gap.

### 2. Build `onDmMessageCreated` Cloud Function
**Files:** `~/Desktop/runcheck-backend/functions/src/onDmMessageCreated.ts` (new), `index.ts`
- Firestore onCreate trigger on `conversations/{conversationId}/messages/{messageId}`.
- Read conversation doc to get `participantIds`. Send push to the participant who is NOT the sender.
- Payload: `data.type = 'dm'`, `data.conversationId`, `data.senderId`, `data.senderName`.
- Respect `notifCooldowns` pattern from Phase 1 functions.
- **Why:** App.js tap handler is already wired. This is the last piece to make DM notifications end-to-end.

### 3. Prepare App Store screenshots
**Where to start:** Simulator (iPhone 15 Pro or iPhone 16 Max, iOS 17/18)
- Screens to capture: HomeScreen, RunDetailsScreen, CheckInScreen, LeaderboardScreen, ProfileScreen, MessagesScreen (at minimum).
- Use the 2026-03-19 TestFlight build on simulator for consistent UI.
- **Why:** Only remaining item on the launch checklist. Has a long tail if screenshots need resizing/framing.

---

## Files / Screens to Start With

- `~/Desktop/runcheck-backend/firestore.rules` — add `conversations` rules + deploy Run Chat rules
- `~/Desktop/runcheck-backend/functions/src/onDmMessageCreated.ts` — new Cloud Function
- `~/Desktop/runcheck-backend/functions/src/index.ts` — export the new function
- Simulator (iPhone 15 Pro) — screenshots after rules are deployed and DMs are testable

---

## What to AVOID Today

- **New features.** The DM system, Run Chat, and push notifications are all built. Do not expand scope.
- **Frontend UI changes.** OTA is broken on the current binary; visual changes can't be verified on device.
- **Refactoring services or hooks** not tied to the above 3 priorities.
- **Working on post-launch items** from `PARKING_LOT.md` (gym image migration, composite Firestore indexes, monetization, etc.).
- **Touching the clip system.** Clips are done and tested. Leave them alone.
