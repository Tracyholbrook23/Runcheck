# Daily Brief — 2026-03-22 (Sunday)

---

## Current Phase
**Pre-launch final QA** — all core features are built. The remaining work is deploying security rules, capturing App Store screenshots, and wiring ToS content. The milestone is: _launch a reliable core run experience for first users in Austin_.

---

## What Is Working

- Run lifecycle: start run, join, leave, merge rule, participant count, empty-run cleanup — complete
- Check-in / check-out flow — complete (GPS re-enabled for production)
- Activity feed (HomeScreen) with stale-run cleanup — complete
- Reliability scoring, rank tiers, session stats — complete
- Clips: record → trim → upload → tag → approve cycle — complete
- Moderation + admin dashboards — complete and deployed
- Push notification infrastructure: 3 Cloud Functions deployed, token registration wired — complete
- Run Chat MVP (`runChatService.js` + `RunChatScreen.js`) — code complete, pending Firestore rules deploy and simulator test
- DM system (`dmService.js`, `MessagesScreen`, `DMConversationScreen`) — code complete, pending Firestore rules deploy
- Onboarding region screen (step 0) — complete
- Swipe tabs + gradient avatar ring — complete (TestFlight build distributed 2026-03-19)
- Open Run flow improvement (one fewer tap from HomeScreen) — complete

---

## What Is In Progress / Pending

| Item | Status |
|------|--------|
| **Firestore rules for `conversations` collection** | Not deployed — DM read/write is unprotected until this ships |
| **Firestore rules for `runs/{runId}/messages`** | Not deployed — Run Chat is blocked on device until this ships |
| **App Store screenshots** | Not captured — last blocking item on launch checklist |
| **Terms of Service content** | Button exists in SettingsScreen but content is not wired |
| **Push notification on-device verification** | Needs TestFlight install + real device confirm (token write, notification delivery) |
| **Swipe tabs on physical device** | Simulator-verified only |
| **QA proximity check-in on physical device** | Not yet done |
| **Fresh iOS build** | Build quota resets ~April 1 — needed to bake `channel: production` into binary for OTA |

---

## Issues / Risks

- **OTA updates broken** — current TestFlight binary is missing the channel header. Any frontend OTA pushes won't reach devices until a fresh build is submitted (~April 1). Use simulator for frontend validation until then.
- **DM conversations are unprotected** — no Firestore security rules deployed for the `conversations` collection. Any authenticated user can read/write any conversation. Deploy rules before any real device testing of DMs.
- **Run Chat rules not deployed** — `runs/{runId}/messages` subcollection access control not live. Same risk as DMs.
- **`notifCooldowns` map will grow unboundedly** — not urgent until ~500 active users; already documented in BACKEND Known Issue #9.

---

## TOP 3 Priorities Today

### 1. Deploy Firestore security rules for `conversations` and `runs/{runId}/messages`
These are the only items blocking safe device testing of the two most recently built features (DM system + Run Chat). Without them, both subcollections are wide open. Deploy before doing any device QA that exercises messaging.

**Files to start with:**
- `firestore.rules` — add rules for `conversations/{conversationId}` (allow read/write if `request.auth.uid` is in the `participants` array) and `conversations/{id}/messages/{msgId}` (same gate); confirm `runs/{runId}/messages/{msgId}` rule grants read/write only to `runParticipants/{runId}_{uid}` doc holders.

### 2. App Store screenshots
The only unchecked launch-checklist item under "App Store Readiness." Required before App Store submission. Capture in simulator or on physical device.

**Screens to capture:** HomeScreen, RunDetailsScreen, ProfileScreen, ViewRunsScreen, CheckInScreen (at minimum — App Store typically requires 3–5 screenshots per device size).

### 3. Wire Terms of Service content in SettingsScreen
The ToS button already exists in `SettingsScreen.js`. It needs real content (URL or inline text) before App Store submission. A URL pointing to a Notion or website page is the fastest path — same pattern already used for the Privacy Policy (`https://www.notion.so/RunCheck-Privacy-Policy-...`).

**File to start with:**
- `screens/SettingsScreen.js` — find the ToS button handler and replace the placeholder with a `Linking.openURL('https://...')` call.

---

## What to AVOID Today

- **`onDmMessageCreated` Cloud Function** — DM push notifications are parked post-launch. Do not build this.
- **Run Chat push notification fan-out** — same. Parked.
- **Verified Run History Phase 2/3** — post-launch, medium-risk schema change. Do not touch.
- **`notifCooldowns` migration** — not urgent until ~500 users. Do not touch.
- **New UI features or visual redesigns** — launch milestone is stability and readiness, not polish.
- **Any new Firestore collections or schema changes** — no schema changes unless a specific bug demands it.
- **Refactors** — even if you see code smells, log them in `PARKING_LOT.md` and move on.

---

_Generated automatically by morning-brief scheduled task — 2026-03-22 07:00_
