# Daily Brief — 2026-03-19

_Auto-generated by morning-brief scheduled task_

---

## Current Phase

**Pre-launch final stretch.** All core systems are built and checked off. The only remaining launch checklist items are App Store screenshots and the TestFlight build. Backend Cloud Functions also need a deployment pass before the build ships.

---

## What Is Working

- Auth, signup, email verification, onboarding — complete
- Check-in / check-out flow — complete (GPS enforcement re-enabled client-side 2026-03-18)
- Start run / join run / leave run — complete (merge rule, participant count floor, empty run cleanup all done)
- Activity feed cleanup (stale run entries, `joined a run at` spam) — complete
- Reliability system (Cloud Functions own writes, client is read-only) — complete
- Schedule no-show detection (`detectNoShows` Cloud Function added) — code complete, needs deploy
- Clips (record, trim, upload, tag, approve, soft-delete, playback error states) — complete
- Admin tools (Reports, Suspended Users, Hidden Clips, Gym Requests, All Clips) — complete
- Suspension enforcement (check-in + run creation blocked for suspended users) — code complete, needs deploy
- Proximity check-in hook (`useProximityCheckIn`) — built and integrated in CheckInScreen and RunDetailsScreen
- Haptic feedback — integrated on check-in, record, clip post
- App icons, splash screen, privacy policy — verified and ready
- `eas.json` version source — set to remote

---

## What Is In Progress

- **TestFlight build** — not yet triggered. Requires native rebuild. Being batched with push notifications setup to avoid two rebuilds.
- **Push notifications** — not implemented. Blocking the TestFlight build batch.
- **App Store screenshots** — not yet captured. Last remaining visual launch task.
- **Backend Cloud Function deploys** — the following need a deploy from `~/Desktop/runcheck-backend` before launch:
  - `detectNoShows` (new scheduled function — every 15 min)
  - `onScheduleWrite` (cancel threshold aligned to 60 min)
  - `checkIn` and `createRun` (suspension guards added 2026-03-17)
- **Proximity check-in UI** — hook is integrated but the visual nudge card in CheckInScreen and RunDetailsScreen has not been verified on-device.

---

## Issues / Risks

- **Backend not deployed** — suspension guards, no-show detection, and cancel threshold alignment are all in code but not live. These are reliability and trust features that must ship before real users onboard.
- **`CreatePrivateRunScreen` is out-of-scope** — this screen was added in yesterday's session outside the launch checklist. It is UI-only (no Firestore writes), so no backend risk, but it adds surface area. Confirm it was intentional before shipping.
- **`useProximityCheckIn` not device-verified** — dismiss cooldown resets on app kill (acceptable for MVP), but the prompt card and CTA flow itself haven't been independently QA'd on device.
- **No tests for yesterday's new files** — `useProximityCheckIn`, `locationUtils`, `haptics`, `AdminAllClipsScreen`, `CreatePrivateRunScreen` have no tests. Not a launch blocker but worth noting.

---

## TOP 3 Priorities for Today

### 1. Deploy backend Cloud Functions
**Why first:** Suspension enforcement, no-show detection, and cancel threshold alignment are not live. These are correctness issues that affect real users from day one.
- `cd ~/Desktop/runcheck-backend`
- `firebase deploy --only functions:checkIn,functions:createRun,functions:onScheduleWrite,functions:detectNoShows`
- Verify each function appears in the Firebase Console after deploy

### 2. Set up push notifications + trigger TestFlight build
**Why second:** TestFlight is the last major launch blocker. Push notifications are being batched into the same native rebuild, so do both together.
- Add `expo-notifications` config to `app.json` (or confirm it's already present)
- Configure notification permissions in the onboarding/permissions flow if not done
- Run `eas build --platform ios --profile development` (or production profile if ready for TestFlight)
- Distribute to TestFlight once build completes

### 3. QA proximity check-in on device + capture App Store screenshots
**Why third:** Both require a working device build. Do proximity QA first (functional), then screenshots (visual).
- Start at `screens/CheckInScreen.js` — verify gym prompt appears when within `checkInRadiusMeters`
- Verify the "Check In" CTA on the nudge card completes a real check-in
- After QA passes, capture all required App Store screenshots in the simulator or on device

---

## Files / Screens to Start With

| Task | Start here |
|------|-----------|
| Backend deploy | Terminal: `~/Desktop/runcheck-backend` |
| Push notifications | `app.json`, then onboarding flow if permissions gate needed |
| Proximity QA | `hooks/useProximityCheckIn.js`, `screens/CheckInScreen.js`, `screens/RunDetailsScreen.js` |
| Screenshots | App running on simulator — no file edits needed |

---

## What to AVOID Today

- **Do not add new features.** The scope gate is active. Park anything new in `PARKING_LOT.md`.
- **Do not touch Firestore schema.** All backend changes today are deploys of already-written code, not schema changes.
- **Do not refactor existing screens** as part of QA — if a bug is found, fix only the specific broken behavior.
- **Do not start a second native rebuild** — push notifications and TestFlight must be bundled into one rebuild to avoid wasted EAS build time.
- **Do not work on `CreatePrivateRunScreen`** — it's UI-only and out of scope. Leave it as-is unless explicitly approved.
